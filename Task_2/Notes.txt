In problem 1, I made a program that lets the user enter numbers.
The program stops when the user enters -1.
I stored all the numbers in a list called numbers.
After the input is finished, I checked if the list is not empty.
If it has values, I used max() to find the largest number and min() to find the smallest number.
If no numbers were entered, I print a message saying: No numbers entered.
From this problem, I learned how to stop the loop using a condition.
In addition, I learned how to store values in a list and find the maximum and minimum values using built-in functions.

In problem 2, the program takes a number from the user.
Then it calculates the sum of all even numbers from 1 up to that number.
I used a loop to go through all numbers, and I checked if the number is even using % 2.
If the number is even, I add it to a variable called total.Finally, I print the result.
I learned how to use loops and conditions together.
I learned how to check if a number is even using the modulo operator %.



In program 6, I take a sentence as input from the user.
Then I use split() to divide the sentence into a list of words.
After that, I use reverse() to reverse the order of the words.
Then I use join() to combine the words back into one sentence.
Finally, I print the new reversed sentence.
I learned how to manipulate strings.
I also learned how to reverse a list.


In problem 3, I take a complex number as input.
Then I use abs() to calculate the modulus, which is the distance.
I also use cmath.phase() to calculate the phase angle.
Finally, I print both values.
I learned how to work with complex numbers in Python.
I also learned how to use the cmath module to calculate modulus and phase.


In problem 4, I take an integer as input.
Then I use if and else if conditions to check different cases.
If the number is odd, I print "Weird".
If it is even, I check its range and print the correct result.
I learned how to check multiple conditions and ranges.


In problem 5, I take a number from the user.
Then I use a loop to print numbers from 1 to n.
I used end ="" to print all numbers in one line without spaces.
I learned how to use loops to repeat actions.
I also learned how to control the output format using end ="".


In linux problem, I used some Linux commands in the terminal.
I used echo to print text, date to show the current time, lsb_release -a to display system information,
and systemd-detect-virt to check if the system is virtual or not.
I learned some basic Linux commands and how to use the terminal.
I also understood what each command does and what its output means.


